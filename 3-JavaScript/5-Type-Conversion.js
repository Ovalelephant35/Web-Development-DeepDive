let age = window.prompt("how are old are You");//string type because of window prompt
age = Number(age);
age+=1;
console.log(age);

//non empty bool return true else it return false "" is empty
//nan for things you want to convert that is not the things specified